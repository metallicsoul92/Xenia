#ifndef KERNEL_H_
#define KERNEL_H_

//#define KERNEL_PHYS_BASE	0x00100000
//#define KERNEL_VIRT_BASE	0xC0000000

//#define EARLY_PHYS_TO_VIRT(x)	(KERNEL_VIRT_BASE + (x))
//#define EARLY_VIRT_TO_PHYS(x)	((x) - KERNEL_VIRT_BASE)

//#define PAGE_SHIFT			12
//#define PAGE_SIZE			(1 << PAGE_SHIFT)
//#define PAGE_ROUND_UP(x)	(((x) + (PAGE_SIZE - 1)) & ~(PAGE_SIZE - 1))

//#define EARLY_STACK_SIZE	PAGE_SIZE
//#define EARLY_HEAP_SIZE		(48 * PAGE_SIZE)


#endif
